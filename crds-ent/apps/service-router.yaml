apiVersion: consul.hashicorp.com/v1alpha1
kind: ServiceRouter
metadata:
  name: frontend
spec:
  routes:
    - match:
        http:
      destination:
        service: frontend
        namespace: apps
        requestTimeout: '30s'

        